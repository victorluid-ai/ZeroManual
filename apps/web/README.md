# Web ZeroManual

Sitio comercial en `zeroman/` — landing, precios, registro de clientes.

Servido en la raiz del API: `http://localhost:8090/`

Ver `docs/brand.md` para la relacion con el panel de operaciones (`/admin`).
