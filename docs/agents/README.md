# Docs de agentes

Los agentes de negocio (`AgentBillingOps`, etc.) se movieron a **OpsCenter**.

Usa el espejo en OpsCenter `docs/agents/` o `.claude/agents/manualzero/` en este repo solo como referencia documental legacy.
