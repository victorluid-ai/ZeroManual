services:
  orchestrator:
    image: python:3.11-slim
    working_dir: /app
    command: sh -c "pip install -e . && uvicorn apps.interface.api:app --host 0.0.0.0 --port 8090 --workers 1"
    ports:
      - "8090:8090"
    volumes:
      - ..:/app
    env_file:
      - ../.env
